package com.example.smtd;

import android.app.Application;

public class TodoApp extends Application {

	/* Classes which extend Application and are defined in the manifest xml as 
	 * the application are infact the main "global" area of the app. Use this space
	 * to define global instances of objects and things.
	 */
	
	
	
}
